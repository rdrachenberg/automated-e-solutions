/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['epic-projects.nyc3.digitaloceanspaces.com', 'https://assets.calendly.com/assets/external/widget.js']}
}

module.exports = nextConfig
